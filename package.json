{
  "name": "todolist-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server -d public 8000",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  }
}
